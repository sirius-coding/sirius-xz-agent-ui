services:
  frontend:
    build:
      context: ..
      dockerfile: Dockerfile.cloud
      args:
        VITE_API_BASE_URL: ""
    container_name: sirius-xz-agent-ui
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "26100:26100"
