services:
  frontend:
    build:
      context: ..
      dockerfile: Dockerfile.cloud
      args:
        VITE_API_BASE_URL: http://223.109.140.60:26000
    container_name: sirius-xz-agent-ui
    restart: unless-stopped
    ports:
      - "26100:26100"
